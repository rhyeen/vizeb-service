const StatusError = require('../../services/errors/status-error');
const log = require('../../services/logger');

module.exports.createCraftedComponent = function(id, style, classes, text) {
  return new Promise((resolve, reject) => {
    return resolve();
  });
}
